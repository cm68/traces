# Project Instructions for Claude

## Building

**Always use `make` to build this project, not `go build`.**

```bash
make
```

Do not use `go build ./...` or similar - it does not work correctly for this project.
